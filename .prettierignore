# Prettier ignores — keep formatting to hand-maintained source only.
# Everything below is generated, git-ignored, or vendored; never format it.

# dependencies / build output
node_modules
packages/*/node_modules
.next
out
build
coverage

# generated registry output (shadcn build)
public/r
public/llms.txt

# registry.json — the `all` item is rewritten by scripts/gen-all-item.mjs with
# JSON.stringify, which disagrees with Prettier on array wrapping. Let the
# generator own its format so check:format isn't flaky after every build.
registry.json

# generated by scripts/gen-examples-registry.mjs (source of truth is the
# content/examples/<slug>/*.tsx files) — generator owns its format.
content/examples/registry.ts

# generated by scripts/sync-registry.mjs (source of truth is registry/)
components
lib/utils.ts
lib/identity.ts
app/foundation.generated.css

# generated lint manifest + mcp data + workspace dist
packages/on-system-core/src/manifest.generated.ts
packages/mcp/src/data.generated.json
packages/*/dist

# lockfiles + misc generated
package-lock.json
*.tsbuildinfo
next-env.d.ts
